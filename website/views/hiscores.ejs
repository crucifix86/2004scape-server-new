<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content.site_title || '2004Scape' %> - Hiscores</title>
    <link rel="shortcut icon" href="/img/favicon.ico">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body {
            margin: 0;
            padding: 0;
            padding-top: 180px; /* Push content below header */
            background: #1a1a1a url('/img/bg2.jpg') repeat;
            color: #e0e0e0;
            font-family: Arial, sans-serif;
        }
        
        .main-container {
            max-width: 900px;
            margin: 0 auto;
            background: rgba(0,0,0,0.9);
            min-height: 100vh;
        }
        
        .content {
            padding: 20px;
        }
        
        .hiscores-table {
            background: rgba(42, 24, 16, 0.95);
            border: 3px solid #8b4513;
            border-radius: 10px;
            overflow: hidden;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
        }
        
        thead {
            background: linear-gradient(to bottom, #8b4513, #654321);
        }
        
        th {
            padding: 15px;
            text-align: left;
            color: #ffd700;
            font-weight: bold;
            text-transform: uppercase;
            font-size: 14px;
            text-shadow: 1px 1px 2px rgba(0,0,0,0.5);
        }
        
        tbody tr {
            border-bottom: 1px solid #8b4513;
            transition: background 0.3s;
        }
        
        tbody tr:hover {
            background: rgba(139, 69, 19, 0.2);
        }
        
        td {
            padding: 12px 15px;
            color: #e0e0e0;
        }
        
        .rank {
            color: #ffd700;
            font-weight: bold;
            font-size: 18px;
        }
        
        .rank-1 { color: #ffd700; }
        .rank-2 { color: #c0c0c0; }
        .rank-3 { color: #cd7f32; }
        
        .staff-badge {
            display: inline-block;
            background: linear-gradient(135deg, #ffd700, #ffed4e);
            color: #1a1a1a;
            padding: 2px 8px;
            border-radius: 3px;
            font-size: 11px;
            margin-left: 10px;
            font-weight: bold;
        }
        
        .btn {
            display: inline-block;
            padding: 10px 20px;
            margin: 0 5px;
            background: #8b4513;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            transition: background 0.3s;
        }
        
        .btn:hover {
            background: #a0522d;
        }
        
        .btn-active {
            background: #ffd700;
            color: #1a1a1a;
            font-weight: bold;
        }
        
        .no-players {
            text-align: center;
            padding: 40px;
            color: #888;
        }
        
        h2 {
            color: #ffd700;
            text-align: center;
            margin-bottom: 20px;
            font-size: 32px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.8);
        }
        
        .fixed-header {
            position: absolute !important;
            top: 0 !important;
            left: 0 !important;
            width: 100% !important;
            z-index: 9999 !important;
            transform: none !important;
            filter: none !important;
            will-change: auto !important;
        }
        
        .fixed-header .header {
            position: static !important;
            transform: none !important;
            filter: none !important;
        }
        
        .fixed-header-content {
            position: static !important;
            transform: none !important;
            filter: none !important;
        }
    </style>
</head>
<body>
    <!-- Fixed header - completely separate from main content -->
    <div class="fixed-header" style="position: absolute; top: 0; left: 0; width: 100%; z-index: 9999;">
        <%- include('./partials/header') %>
    </div>
    
    <!-- Main content container - separate from header -->
    <div class="main-container">
        <div class="content">
            <h2>Hiscores</h2>
            
            <div style="text-align: center; margin-bottom: 20px;">
                <a href="/hiscores?sort=total" class="btn <%= sortBy === 'total' || !sortBy ? 'btn-active' : '' %>">Total Level</a>
                <a href="/hiscores?sort=combat" class="btn <%= sortBy === 'combat' ? 'btn-active' : '' %>">Combat Level</a>
                <a href="/hiscores?sort=wealth" class="btn <%= sortBy === 'wealth' ? 'btn-active' : '' %>">Wealth</a>
            </div>
            
            <div class="hiscores-table">
                <table>
                    <thead>
                        <tr>
                            <th width="80">Rank</th>
                            <th>Player</th>
                            <th width="120">Total Level</th>
                            <th width="120">Combat Level</th>
                            <th width="150">Wealth</th>
                            <th width="150">Total XP</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (players && players.length > 0) { %>
                            <% players.forEach((player, index) => { %>
                                <tr>
                                    <td>
                                        <span class="rank rank-<%= index + 1 %>">
                                            <%= index + 1 %>
                                        </span>
                                    </td>
                                    <td>
                                        <%= player.username %>
                                        <% if (player.staffmodlevel > 0) { %>
                                            <span class="staff-badge">STAFF</span>
                                        <% } %>
                                    </td>
                                    <td><%= player.totalLevel || '-' %></td>
                                    <td><%= player.combatLevel || '-' %></td>
                                    <td><%= player.wealth ? player.wealth.toLocaleString() + ' gp' : '-' %></td>
                                    <td><%= player.totalXp ? player.totalXp.toLocaleString() : '-' %></td>
                                </tr>
                            <% }); %>
                        <% } else { %>
                            <tr>
                                <td colspan="5" class="no-players">
                                    No players ranked yet
                                </td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
        
        <div style="background: #1a1a1a; border-top: 2px solid #8b4513; padding: 20px; text-align: center; color: #888; font-size: 14px;">
            <%= content.footer_text || 'Copyright © 2024 2004Scape - All Rights Reserved' %>
        </div>
    </div>
</body>
</html>